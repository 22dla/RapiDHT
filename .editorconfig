root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
# Spaces, to match .clang-format (BasedOnStyle: WebKit), which is the
# authoritative formatter for C/C++ sources in this repository.
indent_style = space
indent_size = 4
trim_trailing_whitespace = true
